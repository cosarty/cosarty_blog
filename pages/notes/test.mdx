export const meta = {
  author: 'Rich Haines'
}

# My MDX page

This is a list in markdown:

- One
- Two
- Three

```javascript
var a = 'cxn'
```

Checkout my React component:

export default ({ children }) => {

return <div>{children}</div>
}
