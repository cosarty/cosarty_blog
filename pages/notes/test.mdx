export const meta = {
  author: 'Rich Haines'
}

# My MDX page

This is a list in markdown:

- One
- Two
- Three

```javascript
let a = 'cxn'
function name(params) {}
```

`Checkout my React component:`

export default ({ children }) => <div>{children} 你好呀</div>
